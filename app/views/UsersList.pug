extends layout
block content
    .main-content 
        h1 All Users 
        table(border=1)
            for item in data 
                tr  
                    td #{item.UserID}
                    td #{item.username}
                    td 
                        a(href='/User-single/' + item.UserID) #{item.realname} 